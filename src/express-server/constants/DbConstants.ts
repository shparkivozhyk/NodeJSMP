export enum DB {
    "MONGO" = "mongo",
    "SQL" = "sql",
}

export enum DbMessages {
    "APPISLISTENING" = "App listening on port",
    "SUCCESSMESSAGE" = "Connection has been established successfully.",
    "ERRORMESSAGE" = "Unable to connect to the database",
}