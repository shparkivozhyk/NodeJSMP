import config from "./config/config.json";
import { Product, User } from "./models";

console.log(config.name);
console.log('hi there, again and again')
const user = new User();
const product = new Product();
